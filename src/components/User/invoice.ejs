<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Invoice</title>
    <style>
        body {
            font-family: 'Helvetica Neue', 'Helvetica', Arial, sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f9f9f9;
            color: #555;
            line-height: 1.6;
        }

        .invoice-container {
            max-width: 800px;
            margin: auto;
            padding: 20px;
            border: 1px solid #eaeaea;
            box-shadow: 0 0 20px rgba(0, 0, 0, 0.1);
            background-color: #fff;
            border-radius: 8px;
        }

        .invoice-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 20px;
            border-bottom: 2px solid #eaeaea;
            padding-bottom: 10px;
        }

        .invoice-header .title {
            font-size: 36px;
            color: #333;
            font-weight: 700;
        }

        .invoice-header .invoice-details {
            text-align: right;
        }

        .invoice-header .invoice-details span {
            display: block;
        }

        .invoice-body {
            margin-bottom: 20px;
        }

        .invoice-body .billing-info,
        .invoice-body .payment-info {
            width: 100%;
            margin-bottom: 20px;
        }

        .invoice-body .billing-info strong,
        .invoice-body .payment-info strong {
            font-size: 16px;
            color: #333;
        }

        .invoice-body .billing-info p,
        .invoice-body .payment-info p {
            margin: 5px 0;
            color: #777;
        }

        .invoice-table {
            width: 100%;
            border-collapse: collapse;
            margin-bottom: 20px;
        }

        .invoice-table th,
        .invoice-table td {
            padding: 12px;
            border-bottom: 1px solid #eaeaea;
        }

        .invoice-table th {
            background-color: #f7f7f7;
            color: #333;
            font-weight: 700;
        }

        .invoice-table td {
            color: #555;
        }

        .invoice-table .total-row td {
            font-weight: 700;
            font-size: 16px;
            border-top: 2px solid #eaeaea;
        }

        .invoice-footer {
            text-align: center;
            font-size: 14px;
            color: #777;
            margin-top: 30px;
        }

        @media (max-width: 600px) {
            .invoice-header {
                flex-direction: column;
                text-align: center;
            }

            .invoice-header .invoice-details {
                margin-top: 10px;
                text-align: center;
            }

            .invoice-body .billing-info,
            .invoice-body .payment-info {
                text-align: center;
            }

            .invoice-table th,
            .invoice-table td {
                text-align: center;
            }
        }
    </style>
</head>

<body>
    <div class="invoice-container">
        <div class="invoice-header">
            <div class="title">Invoice</div>
            <div class="invoice-details">
                <span>Created: <%= date %></span>
            </div>
        </div>
        <div class="invoice-body">
            <div class="billing-info">
                <strong>Billed To:</strong>
                <p><%= userName %></p>
                <p><%= email %></p>
            </div>
            <div class="payment-info">
                <strong>Payment Details:</strong>
                <p>Transaction ID: <%= transactionNo %></p>
                <p>Amount Paid: $<%= transactionAmount %></p>
            </div>
            <table class="invoice-table">
                <thead>
                    <tr>
                        <th>Description</th>
                        <th>Amount</th>
                    </tr>
                </thead>
                <tbody>
                    <tr>
                        <td>Registration Fee</td>
                        <td>$<%= transactionAmount %></td>
                    </tr>
                    <tr class="total-row">
                        <td>Total:</td>
                        <td>$<%= transactionAmount %></td>
                    </tr>
                </tbody>
            </table>
        </div>
        <div class="invoice-footer">
            <p>Thank you for your payment!</p>
        </div>
    </div>
</body>

</html>